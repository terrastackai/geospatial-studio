# © Copyright IBM Corporation 2025
# SPDX-License-Identifier: Apache-2.0


apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gfm-mlflow.fullname" . }}-test-connection"
  labels:
    {{- include "gfm-mlflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gfm-mlflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
